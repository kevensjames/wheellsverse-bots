openai>=1.30.0
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
schedule>=1.2.0
APScheduler>=3.10.4
pandas>=2.2.0
numpy>=1.26.0
openpyxl>=3.1.0
python-docx>=1.1.0
PyPDF2>=3.0.0
Pillow>=10.3.0
rich>=13.7.0
tinydb>=4.8.0
stripe>=9.9.0
tweepy>=4.14.0
yfinance>=0.2.40
feedparser>=6.0.11
fastapi>=0.116.0    # bumped to allow starlette CVE fix
uvicorn[standard]>=0.29.0
python-dateutil>=2.9.0
pytz>=2024.1
pyyaml>=6.0.1
jinja2>=3.1.4
markdown>=3.6.0
reportlab>=4.2.0
anthropic>=0.39.0
supabase>=2.4.0
python-multipart>=0.0.9
psutil>=5.9.0

# NarAI v2 — auth
bcrypt>=4.0.0
passlib[bcrypt]>=1.7.4
PyJWT>=2.8.0

# NarAI v2 — chat/memory/rag pipeline (Steps 1–7 + Telegram integration)
# chromadb backs the episode store and RAG; litellm is the LLM router;
# sqlalchemy[asyncio]+aiosqlite back the facts/patterns SQL store.
chromadb>=0.5.0
litellm>=1.83.0   # GHSA-jjhc-v7c2-5hh6 (Critical) + 3 High advisories survive through 1.80
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0

# NarAI v2 — Step 5 voice (TTS only; STT uses existing openai package;
# ElevenLabs is optional and imported lazily only when NARAI_TTS_PROVIDER=elevenlabs)
edge-tts>=6.1.0

# NarAI Godmode — Google OAuth (Gmail/Drive/Calendar/YouTube/Sheets/Docs)
# cryptography is explicit (transitively pulled by python-jose, but narai_godmode.vault
# imports it directly — declaring keeps the dependency obvious)
cryptography>=42.0.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.130.0
google-auth-httplib2>=0.2.0
