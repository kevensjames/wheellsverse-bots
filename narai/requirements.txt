# NarAI Phase 0 dependencies
# Install: pip install -r narai/requirements.txt

# Multi-model router
litellm>=1.40.0

# Vector memory
chromadb>=0.5.0

# RAG
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
pypdf>=4.0.0

# DB
sqlalchemy[asyncio]>=2.0.0
alembic>=1.13.0
aiosqlite>=0.20.0

# API + auth
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-jose[cryptography]>=3.4.0  # CVE-2024-33663/33664 (algorithm confusion + DoS) fixed in 3.4.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.20  # CVE-2024-53981 (DoS via slow uploads) fixed in 0.0.18+

# Resilience
tenacity>=8.3.0
pybreaker>=1.2.0

# Encryption
cryptography>=42.0.0

# Embeddings (local, Apple Silicon)
sentence-transformers>=3.0.0

# Already in root requirements.txt — kept for standalone installs
openai>=1.30.0
anthropic>=0.28.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
