# KAI OMNIPRESENT HOLDING COMMAND OS — Evidence Matrix (§162)

> Mandated by spec §162: mechanically-traceable evidence per requirement.
> Columns: requirement · implementation · unit_test · integration_test · adversarial_proof ·
> runtime_proof · browser_proof · deployment · SHA · status.
>
> **This is a living doc and it is honest.** Reconciled 2026-09-05 against HEAD `8799db9` on
> `feat/kai-cyber-operations` (worktree `~/wheellsverse-cyberops`, working tree clean). The former
> "pending rows" section is gone: every phase and domain below carries the evidence that actually
> exists. Where a proof has not landed yet the cell says so in words, not `pending`.
> Status mirrors `KAI_OMNIPRESENCE_REQUIREMENTS.md` (§161).

## Status vocab
`SATISFIED` · `PARTIAL` · `GAP` · `DEFERRED` · `N-A` — as defined in the §161 ledger.
`—` = not applicable to this row.

## Deployment rule for this build
**NOTHING DEPLOYED, NOTHING MERGED, NO PR.** Every row's `deployment` cell reads
`undeployed — dark on feat/kai-cyber-operations` except the **§2** row, which records the
pre-existing production floor (release #67, `4fbfb8e`) that this build does not touch.
Deploy ≠ enable: all 9 authority flags default `False` in `backend/app/config.py`.

## Test-harness note
Zero-framework runners raise `SystemExit` at import, so pytest over the whole `app/` tree
INTERNALERRORs — **per-file execution is the harness**. Counts below were re-measured at HEAD with
`DATABASE_URL` + `PYTHONPATH=<repo root>` set (the repo root is required for
`core.security_scanner`, the single pattern base used by `os_lab/certification.py`).

---

## Matrix — phases and domains

| requirement | implementation | unit_test | integration_test | adversarial_proof | runtime_proof | browser_proof | deployment | SHA | status |
|-------------|----------------|-----------|------------------|-------------------|---------------|---------------|------------|-----|--------|
| §2 Current release (production floor) | Holding OS dashboard + `kai-presence.js` (orb + governed streaming drawer) + Nexus + `kai_bridge.py` + deployment-truth registry | suite green at release | bridge App A↔App B verified | RBAC hardening on the merge lineage: 2 HIGH escalations found + fixed | BOTH prod services healthy; authority OFF/MOCK/IN_SYNC; `report_value`→UNAVAILABLE for un-sourced money | **Playwright 4-viewport cert** (3440×1440 / 1920×1080 / 1440×900 / 390×844) | **LIVE — App A `app.wheellsverse.com` + App B `kai-prod`** (unchanged by this build) | `4fbfb8e` (PR #67) | SATISFIED |
| §48 Security center (Cyber Ops A+B) | `services/security/{evidence_bus,posture,risk_score,aikido_adapter}.py` + `routers/admin_security.py` (10 read-only `/admin/cyber/*`) + `cyber-operations.html` + `kai-nexus-security.js` | `test_security` **44/44** (re-run at `83f6050`; `services/security/` untouched since) | holding cyber card consumes the same endpoints | 6-lens adversarial review-triad clean | flag `KAI_CYBER_OPS_ENABLED` default False; Aikido/monitor report NOT_CONNECTED, incidents PHASE_C_PENDING | `test_nexus_security` 24 (node) | undeployed — dark on `feat/kai-cyber-operations` | `0fed354` + `6898394` | SATISFIED (phases C–F not built) |
| §70/§73 Visual language + offline/degraded | `holding.html`, `cyber-operations.html`, `kai-nexus.css` (Space Grotesk, navy/cyan, original KAI identity); AbortController → NOT_CONNECTED / OFFLINE / DEGRADED, no fake AI on disconnect | `test_holding_ui_contract` 7/7 | panels degrade independently | honesty pattern verified: with no backend the whole page renders "No value is fabricated" | 9 console errors, all expected fetch-404s; 0 JS exceptions | Playwright desktop 1920 + mobile 390 (P6b-2) | undeployed — dark on `feat/kai-cyber-operations` | `4d7999d` + `543da39` | PARTIAL (formal degraded cert not run) |
| **P0 §161–§164 governance docs** | `docs/KAI_OMNIPRESENCE_{BASELINE,REQUIREMENTS,EVIDENCE_MATRIX,DECISIONS,THREAT_MODEL}.md` | counted at HEAD: **9 ADRs**, **19 threat rows**, 8 boundaries | ledger ↔ matrix cross-reference (this reconciliation) | 4 flagged HIGH gaps + residual-risk register with a §0#12 re-score trigger | — | — | undeployed — dark on `feat/kai-cyber-operations` | `d881cf2` | SATISFIED |
| **P1 §1/§14/§15/§16/§62/§63/§99/§141** self-model · twin · knowledge index | `self_model.py` (full §62 field set, live-derived limitations, env-honest SHA), `digital_twin.py` (hierarchy edges, confidence + evidence_ref, single-sourced money_mode), `knowledge_index.py` (NEW) | `test_omnipresence_phase1` **35/35**, `test_self_model` **8**, `test_digital_twin` **9** | touched suites (self_model / digital_twin / holding_view / ui_contract / registry) green | independent review: correctness PASS; truth PASS after 6 fixes (1 HIGH SHA-honesty + 5 LOW) applied in the commit | `claims_consciousness=False` asserted; staging never mislabels prod | self-model panel rendered (P6b-2 probe) | undeployed — dark on `feat/kai-cyber-operations` | `d881cf2` | PARTIAL (§1 has no single ten-submodel accessor; §14 money/customer facts UNAVAILABLE pending operator provisioning) |
| **P2 §17/§32/§81/§82** attention · goals · memory | `attention_model.py`, `goal_registry.py` (own `goals.db`), `holding_memory.py` (own `holding_memories` table, no profiles FK) | `test_attention_model` **24/24**, `test_goal_registry` **28/28**, `test_holding_memory` **14** | 5 pre-existing DB-required holding failures identical on the baseline (git-stash comparison) | truth review PASS; correctness review PASS after the HIGH profiles-FK fix + 5 LOW | honest IDLE — never invents a mission; provenance-required write-path refuses silent LLM→fact | Current Attention panel (P6b-2 probe) | undeployed — dark on `feat/kai-cyber-operations` | `8be9f2d` | SATISFIED |
| **P3a §18/§22/§55** unified problem · single ranker · cross-company | `holding_problems.py` (wraps `priorities` + `self_improvement_detect`), `priorities.rank_key` as THE 11-rung ladder (3 rankers → 1), `cross_company.py` | `test_holding_problems` **19/19**, `test_cross_company` **23/23**, `test_proposals` **10/10** | `briefing._oa_key` and `proposals.build_daily_plan` delegate to the one ranker (local maps deleted) | consolidation review PASS (streams wrapped, not forked); truth pass clean — one ranker, no LLM, no regression | ≥2 possible causes always, never one fabricated root; honest empty for cross-company | Problems panel (P6b-2 probe) | undeployed — dark on `feat/kai-cyber-operations` | `8b9c2fe` | PARTIAL (doc-inaccuracy source returns honest `[]` until connected) |
| **P3b §11/§12/§19/§20/§21/§31/§83/§84/§85** opportunity · idea · notification · proactive · arrival | `opportunity_engine.py`, `proposals_store.py` (`kind` + `evidence_sig` on the ONE table), `notification_policy.py`, `proactive_engine.py`, `arrival.py`, `reports.py` (weekly + deep dive) | `test_opportunity_engine` **7**, `test_ideas` **18/18**, `test_notification_policy` **18/18**, `test_proactive_engine` **19/19**, `test_arrival` **16/16**, `test_weekly_review` **14/14**, `test_company_deep_dive` **11/11** | 14-suite regression sweep green on local `wheellsverse_test` PG; `test_proposals` 10/10, `test_owner_queue` 7 | review triad: truth PASS; consolidation PASS after 1 MEDIUM (`upsert_owner_open` kind-scoping) + 1 LOW (blank evidence sig) fixed in the commit | ideas never leak into owner decisions (proven on a mixed table); `KAI_PROACTIVE_ENABLED` default OFF | Opportunities + Decisions panels (P6b-2 probe) | undeployed — dark on `feat/kai-cyber-operations` | `261931e` | SATISFIED |
| **P4 §26/§27/§28/§29/§30** mission aggregate · working-now · dark scheduler | `mission.py` (thin header + derived 11-state lifecycle + atomic partial unique index), `holding_cycle.beat_schedule_entry`, `workers/celery_app.py` reuse | `test_mission` **52/52**, `test_holding_schedule` **7/7**, `test_holding_cycle` **10**, `test_manual_cycle` **7** | mission view joins PlanTask + proposals + worker_jobs + WorkResult + CycleRecord via the pre-existing `mission_id`; one hoisted fetch, no N+1 | review triad: consolidation PASS; truth PASS after the §26 COMPLETE⟹evidence consistency fix + 3 LOW, re-review clean | COMPLETE-with-empty-evidence downgraded to READY_FOR_REVIEW; raced duplicate suppressed not crashed; `KAI_HOLDING_CYCLE_ENABLED` default OFF (no new daemon) | Working-Now + Missions panels (P6b-2 probe) | undeployed — dark on `feat/kai-cyber-operations` | `d395f24` | SATISFIED |
| **P5 §8/§24/§54/§76/§90/§91** typed Command API · streaming · NL router · approval · injection fence | `command_router.py`, `routers/admin_holding_command.py` (`/command`, `/command/confirm`, `/command/stream`), `approval_dialog.py`, `results.neutralize_untrusted_context` wired at `nai_brain.brain._holding_context` | `test_omnipresence_phase5` **22/22**, `test_approval_dialog` **43/43** | typed dispatch to the existing Brain / KnowledgeIndex / shared execution service — no second plane; SSE reuses `admin_chat._sse_async` | security review PASS after 2 MEDIUM (reject write-integrity, fence-sentinel injection) + 1 LOW fixed in the commit; consolidation PASS | typed-not-shell (no eval/exec/subprocess on the command string); body role ignored ⇒ 403; consequential ⇒ 202 REQUIRE_APPROVAL; route 404 when the flag is off; adversarial fence-breakout cannot escape the per-call nonce | — | undeployed — dark on `feat/kai-cyber-operations` | `6247f22` | PARTIAL (§90 spec tail truncated — envelope completeness unconfirmable) |
| **P6a §56/§57/§58/§60/§61** health score · system graph · timeline · approval package | `health_score.py`, `system_graph.py`, `timeline.py`, `approval_package.py` | `test_health_score` **16/16**, `test_system_graph` **30/30**, `test_timeline` **60/60** (re-measured on `release/kai-holding-os`; was 34/34 at `12a28f6`), `test_approval_package` **15/15** | `self_improvement.owner_review_package` now reuses `approval_package` (no fork). **Timeline ingestion CORRECTED:** this row previously read "timeline ingests audit/mission/deploy/security" — the adapters existed but **no caller invoked `ingest()`** anywhere outside `test_timeline.py`, so the §61 panel was empty by construction. On the release candidate `timeline.view()` is the router's one seam and ingests on the READ path from audit_log · mission · **proposals_store (new adapter)** · deployment SHA · evidence_bus, returning `store` + per-`sources` status with the events | review: consolidation PASS; truth PASS after 1 MEDIUM (security-absence-as-healthy) + 4 LOW (min-measurable floor, evidence-quality unknown-age, timeline provenance ×3) fixed in the commit | a dimension with no source is INSUFFICIENT_DATA and EXCLUDED; <3 measurable ⇒ NO_SCORE; dangling/self-loop edges refused; synthesized ts tagged DERIVED. Timeline: an unreadable source is reported UNAVAILABLE and the panel says so — an empty timeline is never rendered as "nothing happened"; a proposal/audit/mission row with no timestamp of its own contributes nothing rather than a placeholder | System Health / Timeline / System Graph panels (P6b-2 probe); Timeline panel's three states asserted by `frontend/admin/test_timeline_panel.js` **6/6** (node, renders the real `renderTimeline`) | undeployed — dark on `feat/kai-cyber-operations`; timeline wiring on `release/kai-holding-os` | `12a28f6` + timeline wiring on `release/kai-holding-os` | PARTIAL (§61: `security.evidence_bus` is not in this build ⇒ that source is permanently UNAVAILABLE; ingest runs on read only, no scheduler per §79; `worker_execution` has no producer) |
| **P6b-1** 12 owner-gated read endpoints | `routers/admin_holding.py` — `/health`, `/system-graph`, `/timeline`, `/attention`, `/missions`, `/problems`, `/cross-company`, `/opportunities`, `/goals`, `/knowledge`, `/proactive`, `/system-model` (+ `/view` folds them in) | broad suite sweep green (holding_view, ui_contract, health 16/16, mission 52/52, security 44/44) | each endpoint calls ONE service fn; `/view` uses a shared mission builder, not a duplicate | security + truth + no-regression review PASS, **0 findings** | verified at HEAD: 12 endpoints, all `GET` + `Depends(require_kai_ultra)`, 0 new POST, no execution/mutation reachable; fail-soft to an honest marker (never a 500 hiding state) | — | undeployed — dark on `feat/kai-cyber-operations` | `c3e3f8c` | SATISFIED |
| **P6b-2 §3/§45/§46/§47/§53/§59/§70/§71/§72/§144/§148** command-center dashboard | `frontend/admin/holding.html` — 19 panels, evidence drawer (§59), command palette (§53), a11y pass, responsive grid | `test_holding_ui_contract` 7/7 (after `543da39`) | fed by the P6b-1 endpoints through the App A→App B bridge `/admin/kai/holding/*` | review-triad agents hit the session limit; verification done inline — `node --check` on the 49KB inline JS passes; a11y / zero-fake / XSS greps confirmed | every value is a live fetch or an explicit honest marker (NOT_CONNECTED / UNAVAILABLE / INSUFFICIENT_DATA / PHASE_C_PENDING / REQUIRES_OPERATOR_CONFIRMATION) | **Playwright desktop 1920 + mobile 390**: every panel renders honest NOT_CONNECTED with no backend, 0 JS exceptions, no horizontal overflow; measured at HEAD — 69 aria/role attributes (was ZERO), `esc()` on all untrusted data | undeployed — dark on `feat/kai-cyber-operations` | `4d7999d` | PARTIAL (VISION/GESTURE, OS-LAB and BRAKES/STOP panels absent; §53 palette defers Ask to the presence drawer) |
| **P7a §6/§7/§67/§68/§75/§92/§128–§130/§135** VoiceSessionManager | `holding/voice_session.py` — one turn (guards → FINAL transcript text → §8 router → response → approval → speech → §92 audit); `config.KAI_VOICE_ENABLED = False` | `test_voice_session` **36/36** (§135 subset: permission-denied / mic-unavailable / PTT / timeout / transcription-fail / barge-in / mute / session-expiry / duplicate / unauthorized / ambiguous-approval-refused / stop) | routes through the SAME §8 `command_router` as the Holding Command API; `confirm_by_voice` delegates to `approval_dialog` | independent review (privacy + security + consolidation) **PASS, 0 findings** | backend never receives audio (text transcript only); `core/wake_word_listener.py` never imported (asserted); WAKE_WORD_LOCAL UNAVAILABLE — a cloud engine is REFUSED; §92 audit is an exact 12-key redacted allowlist | — | undeployed — dark on `feat/kai-cyber-operations` | `bee61fd` | PARTIAL (no local wake-word engine; never run against a live microphone) |
| **P7b §6/§7/§10/§49/§51/§52/§64/§66/§67/§68/§69/§75** voice frontend · embodiment · Nexus consolidation | `kai-presence.js` (voice bar, indicators, one governed dispatcher, §75 approval card, §66 arrival, §67 settings, §68 quiet/mute, avatar wiring); `nexus.html` deleted, `/admin/nexus` → 307 → `/admin/mission-nexus`; `GET /admin/holding/voice/capabilities` | 17/17 frontend suites at the time; **18 suites / 258 checks at HEAD**; `test_voice_session` 36/36 | one immersive view hosts the shared provider via slots | 2 lenses → **1 HIGH + 7 MED + 10 LOW found, all fixed**; post-commit refuter: **11/12 hold**, 1 MED (`voice.start` trigger passthrough) → fixed in `efb05c6` | GLB ASSET_UNAVAILABLE → VIDEO fallback, no biometric inference; TTS gated on backend `KAI_VOICE_ENABLED` truth; `CANCEL_UNAVAILABLE` reported instead of a shadow controller | **headless Playwright probe: 0 recognizers, 0 POSTs without a press** | undeployed — dark on `feat/kai-cyber-operations` | `85b5de0` | PARTIAL (GLB asset missing; §50 personality layer absent) |
| **P8 §8/§9/§93/§94/§130/§136** camera + gesture layer | `holding/gesture_policy.py` (5-gesture vocabulary; CONSEQUENTIAL never returnable — asserted; `camera_allowed` = flag AND explicit session enable; `FRAMES_LEAVE_DEVICE=False`, `BIOMETRIC_INFERENCE=False`), `frontend/admin/kai-gesture.js` (the ONE `getUserMedia` site), `config.KAI_CAMERA_ENABLED = False`, `GET /admin/holding/gesture/capabilities` | `test_gesture_policy` **55/55**, `test_kai_gesture` **27**, 18 frontend suites / **263** checks (all re-measured at HEAD `749fa78`; were 51 / 22 / 258 before `9e0df08`) | recognizer seam gated on backend certification truth (`17ec7ce`) — the frontend can no longer report REGISTERED while the backend says otherwise; three of the new `test_kai_gesture` checks exercise the **real async-gap race** in `start()` | consolidation lens **PASS with 1 MEDIUM** → fixed in `17ec7ce`. Privacy/authority/safety lens: **FAIL — 2 HIGH + 2 MED + 1 LOW → ALL FIXED in `9e0df08`** (camera gate + visibility re-read in the `getUserMedia` resolve handler ⇒ `STOPPED_DURING_START`; `!e.isTrusted` refused on the §67 camera control **and** the `ptt-press`/`session-button` mic handlers; CAMERA-ON indicator rebuilt when missing or `!isConnected`; non-finite confidence fails closed in `map_gesture`). Lens also proved clean under attack: **no** gesture path reaches a consequential action (vocabulary strings, `'approve'`, prototype keys, crafted event with its own `action`/`handler`, string/`NaN`/`±Infinity` confidence, non-owner roles — only the five injected non-consequential helpers ran); gesture-channel approval hits `REFUSED_CHANNEL` **before verb parsing**, writing nothing durable; `/gesture/capabilities` owner-only + dormant; exactly ONE `getUserMedia` site with zero frame/network/storage APIs near the stream; live `MediaStream` unreachable from the page (module scope); persisted `camera_enabled:true`, `KAI.settings.set(...)` and `KAI.gesture.start()` all neutralized; backend `camera_allowed` rejects truthy non-bools | recognizer seam reports `RECOGNIZER_UNAVAILABLE_NOT_CERTIFIED` — **no model shipped**; 10-key audit event with no frame/image/embedding field; approval by gesture ⇒ REFUSED_CHANNEL; `KAI_CAMERA_ENABLED` still `False` — the fixes hardened the path, they did not enable it | **headless Playwright 62/62** — 0 `getUserMedia` without a real owner click on `/admin/holding` and `/admin/kai-nexus`, exactly 1 after it, stopped on hidden (measured at `efb05c6`; not re-run after `9e0df08`, whose async-gap coverage is the three node tests) | undeployed — dark on `feat/kai-cyber-operations` | `efb05c6` + `17ec7ce` + `9e0df08` | PARTIAL — **not** upgraded: the privacy lens is closed, but there is still **no recognizer model** (uncertified seam) and `KAI_CAMERA_ENABLED` is `False`, so nothing here has ever run against a live camera |
| **P9 §34/§78/§80/§87/§88/§89/§97/§98/§99/§118/§119/§154** governance maturation | `eval_harness.py`, `challenge.py`, `review_panel.py`, `resource_governor.py`, `brakes.py`, `explain.py`, `capabilities_answer.py`, `what_i_did.py`, `worker_health.py`, `improvement_cycle.py`, `timeline.py`; `docs/KAI_OMNIPRESENCE_COMPARTMENTALIZATION.md` (§44) | 11 modules re-measured at HEAD: brakes **63/63**, capabilities_answer **45/45**, challenge **27/27**, eval_harness **36/36**, explain **32/32**, improvement_cycle **24/24**, resource_governor **43/43**, review_panel **28/28**, what_i_did **32/32**, worker_health **32/32**, timeline **34/34**, status **8/8** | STOP wired into `holding_cycle.build_live_engine` and `a2_dispatch.enqueue_a2_coding_job`; one reviewer≠author rule (`coding.assert_independent_reviewer`) reused by `review_panel` and `challenge` | 2 lenses → **3 HIGH + 8 MED + 4 LOW → fixed in `ac9fcff`** (291→394 checks); refuter: **8/9 probes hold**, 1 MED (MONEY_MODE reader default narrated) + 2 LOW → **fixed in `83f6050`** (MONEY_MODE tri-state, panel clamp on every exit, `autonomy_status` derived not hardcoded) | brakes fail closed (unreadable ⇒ engaged); FINANCIAL brake derives from the real Sol/Dwolla switches; an empty audit log never proves 0 violations; restated agreement ⇒ REJECTED_SYCOPHANTIC; missing panel role ⇒ INCOMPLETE | — | undeployed — dark on `feat/kai-cyber-operations` | `6fa5119` + `ac9fcff` + `83f6050` | PARTIAL (§97 has no dashboard STOP board and no `/brakes` endpoint) |
| **P10 §39–§44/§113–§117/§143/§150/§165** governed Systems/OS Lab | `holding/os_lab/` — `catalog.py` (11-entry catalog + §116 dispositions + §113 lifecycle), `runtimes.py` (§40 Ultron, §42 virtme-ng, §43 syzkaller, §165 `OsLabAuthorityGuard`, §102/§150 rows), `certification.py` (26-step pipeline + bounded verdicts), `fixtures/mimic_repo/` (7 §143 benign mimics) | re-measured at HEAD `8799db9`: `test_os_lab_catalog` **83/83**, `test_os_lab_runtimes` **92/92**, `test_certification` **80/80** — **255** total, up from 226 (76 · 87 · 63) at `749fa78` and 216 (73 · 85 · 58) at `83f6050`; the 80/80 **still requires the repo root on `PYTHONPATH`** for `core.security_scanner`, and `pytest` collects all three | `core.security_scanner` is the single pattern base (duplicated regexes deleted; unavailable ⇒ sweep UNVERIFIED); catalog binding checks Ultron source consistency | 2 lenses → **1 HIGH + 8 MED + 7 LOW → fixed in `58a4227`** (171→216 checks). Refuter: **all 16 requested probes HOLD**, 7 new findings (1 HIGH credential-snippet leak, 2 MED, 4 LOW) → **round-2 fix `749fa78`** (216→226) — believed final, and it was not. **Four iterated refuter rounds followed, each attacking the previous round's fix**, closing at **`8799db9`** (255): `ed00e4e` (snippet boundary moved from the label to the **match length** after a greedy match put 101 windows of a 120-char secret in the report; severity gate made fail-closed; **the round-2 `MappingProxyType` freeze reverted** — it had silently disabled `redact()` on evidence written to the audited history), `79062c4` (evidence redacted **at the door** via `StepResult.__post_init__`; malformed findings refused; credential severity floor; pre-context dropped; `EXECUTED` ledger hardened), `8799db9` (seven credential families the tables never matched; the false universal claim narrowed to per-listed-family, gap named **R7**). **No refuter is in flight and no review cell is open in the doc set** | **nothing cloned / downloaded / installed / built / QEMU-booted**; AST scan proves no subprocess/network/git in the package; build/network/QEMU steps SKIPPED `EXECUTION_GATED`; syzkaller `selectable() == False`; Ultron source OPERATOR-STATED and UNVERIFIED; `MALWARE_FREE` unclaimable | — | undeployed — dark on `feat/kai-cyber-operations` | `cd3b96d` + `58a4227` + `749fa78` + `ed00e4e` + `79062c4` + `8799db9` | PARTIAL — **not** upgraded: the report-integrity line is closed after five rounds, but residual **R7** stands (credential detection is a pattern list, not proof — the reason certification N1 reads PARTIAL), there is still no OS-Lab dashboard view (`os_lab_view()` has no router caller), the §165 guard is still **not wired** into any execution seam (`749fa78` normalized its matching and `ed00e4e` anchored its prefix — neither wired it), and nothing has ever been cloned / built / booted |
| **UI-contract regression (found + fixed)** | The §159 sweep found `test_holding_ui_contract` (7/7 on the pre-omnipresence base) FAILING after `4d7999d`: the `kai_working.ready_for_review` bucket and the `self_improvement_ready` block were no longer rendered although `/admin/kai/holding/view` still returns them. Restored inside `renderWorking` (+16/−2, `esc()` on every field, existing classes only) | `test_holding_ui_contract` **7/7** restored; test re-pinned from the old `renderView/renderStatus/onProposalAction` identifiers to `boot()`'s fan-out, first-`<section>` ordering, `renderCompanies`/`renderAutonomy` fields | semantic-by-semantic audit of the rewritten `holding.html`: 6 of 7 contract semantics held, 1 did not | added a **mutation-checked sweep for bare (un-escaped) interpolations** | the dropped surfaces render again | — | undeployed — dark on `feat/kai-cyber-operations` | `543da39` | SATISFIED |
| **Regression sweep (whole build vs. baseline)** | 133 test files outside `holding/` — capability, security, registry, router, nai_brain, App B `tests/`, App A `tests/` | run **per-file on HEAD and on `d881cf2^`** | — | — | **ZERO diff.** The App B `tests/*.py` fixture errors (admin_chat 15, auth 16, browser 59, planning 95, …) are identical on the baseline = pre-existing environment errors, not regressions | — | undeployed — dark on `feat/kai-cyber-operations` | `83f6050` vs `d881cf2^` | SATISFIED |
| **Holding + OS-Lab suite sweep (this reconciliation)** | `backend/app/services/holding/` + `os_lab/` | **67 test modules, 1521 checks, 1480 PASS** at HEAD `8799db9` — the three `os_lab/` suites re-measured at HEAD (226→**255**) on top of the 1452/1451 measured per-file at `749fa78`; `git diff --stat 749fa78..8799db9` touches only `os_lab/` + the OS-Lab doc, so the rest is carried forward on that evidence, not re-swept (was **1437 / 1436 PASS** at `83f6050`) | — | — | the single failure is `test_si_calc_guard` `bucket(0)` (6 passed, 1 failed) — the deliberate BEFORE-state self-improvement fixture, which fails identically on `d881cf2^` | — | undeployed — dark on `feat/kai-cyber-operations` | `8799db9` | SATISFIED |
| **Frontend suite sweep** | `frontend/admin/test_*.js` | **18 node suites, 263 checks**, all pass (incl. `test_kai_gesture` **27**, `test_kai_speech_input` 11) — was 258 at `83f6050` | — | — | run with `node` at HEAD `8799db9` (unchanged since `749fa78` — no frontend file is touched by `ed00e4e`/`79062c4`/`8799db9`) | covers presence, gesture, speech, avatar/GLB/viseme, subtitles, and the 9 Nexus modules | undeployed — dark on `feat/kai-cyber-operations` | `8799db9` | SATISFIED |
| **Authority / flag posture** | `backend/app/config.py` — `KAI_HOLDING_COMMAND_ENABLED`, `KAI_VOICE_ENABLED`, `KAI_CAMERA_ENABLED`, `KAI_CYBER_OPS_ENABLED`, `KAI_HOLDING_CYCLE_ENABLED`, `KAI_PROACTIVE_ENABLED`, `KAI_CAPABILITY_EXECUTION_ENABLED`, `HOLDING_AUTONOMY_ENABLED`, `KAI_A2_EXECUTION_ENABLED` | `test_capabilities_answer` 45/45, `test_brakes` 63/63, `test_status` 8/8, `test_holding_schedule` 7/7 | STOP is an input to every capability/worker row | §97 STOP owner-only and fail-closed | **all 9 default `False`, verified by reading `config.py` at HEAD** (lines 64/72/79/83/101/106/111/115/128); `MONEY_MODE` is not declared in App B Settings ⇒ reported UNAVAILABLE and the FINANCIAL brake derives from `KAI_SCOPE_SOL_TRANSFER` / `DWOLLA_ENV` / `DWOLLA_ALLOW_PRODUCTION`; `config.py` is untouched by `9e0df08`, `749fa78`, `ed00e4e`, `79062c4` and `8799db9`, so the posture is unchanged at HEAD | — | undeployed — dark on `feat/kai-cyber-operations` | `83f6050` (read) · unchanged at `8799db9` | SATISFIED |
| **§134/§152/§157/§159 hosted + deployment tiers** | — | — | — | — | **no isolated Railway staging environment exists** | — | undeployed — dark on `feat/kai-cyber-operations`; no PR, nothing merged | — | DEFERRED `[G:STAGING]` |
| **§153 rollback plan for this build** | `docs/ROLLBACK_ceo_dashboard.md` is **not present in this worktree**; `holding_deployment.py` records no previous SHA | — | — | — | — | — | undeployed — dark on `feat/kai-cyber-operations` | — | GAP |

**Row count: 24.**

---

## Gates remaining (operator-only)

| gate | why it blocks | what it unblocks |
|------|---------------|------------------|
| Provision an isolated Railway staging environment | none exists | §134 HOSTED tier · §152 staging deploy · §157/§159 hosted certification |
| Hosted edge certification on staging | depends on the gate above | §159 final certification report |
| Production merge/deploy go-ahead | nothing merged, no PR | any deployment claim in this matrix |
| Any authority enable (deploy ≠ enable) | all 9 flags default False | §23 A2+ · §26 live execution loop · §7 live voice · §9/§94 live camera |
| Credential / finance / customer data provisioning | `report_value` → UNAVAILABLE | §13 brief sub-sections · §45/§46 finance & customer · §123 those event feeds |
| Restricted-security activation | syzkaller is never auto-selected (`selectable() == False`) | §43 |

## Adversarial-review posture at HEAD

| phase | lenses | findings | fix SHA | refuter verdict |
|-------|--------|----------|---------|-----------------|
| P1–P7a | in-session review triads before each commit | see each commit body | in-commit | truth/correctness/consolidation PASS per phase |
| P7b | 2 | 1 HIGH + 7 MED + 10 LOW | in-commit `85b5de0` | 11/12 hold; 1 MED fixed in `efb05c6` |
| P8 | consolidation | 1 MED | `17ec7ce` | consolidation lens PASS |
| P8 | privacy / authority / safety | **FAIL — 5: 2 HIGH** (camera-lifecycle async gap: the gate was never re-validated after the lazy-load + `getUserMedia` await, so a stop / sign-out / mute / tab-hide in that window was lost and the camera turned ON afterwards, including in an already-hidden tab · camera-activation: the §67 change handler accepted script-dispatched events, letting a page script piggyback any unrelated real click to mint `'owner-click'` with the panel closed) **+ 2 MED** (indicator: the mandatory CAMERA-ON banner was cached, so a detached node meant a live camera with no indicator · mic-activation: same untrusted-event root cause on the mic DOM path — code-path finding, **not** runtime-verified) **+ 1 LOW** (`gesture_policy.map_gesture`: `NaN` beat every comparison and was MAPPED while the frontend refused it as MALFORMED — seam drift) | **all 5 fixed `9e0df08`** | **CLOSED.** Post-fix `test_gesture_policy` **55**, `test_kai_gesture` **27**, 18/18 frontend suites, `test_voice_session` 36/36, `test_approval_dialog` 43/43. Lens also proved the gesture→action, approval-channel, single-`getUserMedia`, stream-unreachability and persisted-flag-neutralization properties clean under attack |
| P9 | 2 | 3 HIGH + 8 MED + 4 LOW | `ac9fcff` | 8/9 probes hold; 1 MED + 2 LOW fixed in `83f6050` |
| P10 | 2 + **4 iterated refuter rounds** | 1 HIGH + 8 MED + 7 LOW (lenses) → then **4 HIGH + 1 self-inflicted regression** across the refuter rounds | `58a4227` → `749fa78` → `ed00e4e` → `79062c4` → `8799db9` | 16/16 requested probes hold. Then four independent refuters, **each attacking the previous round's fix**, each found a new route to the same class of defect: **R2** (`749fa78`) findings embedded the matched secret, truncated before `redact()` — AWS `ASIA`/`AROA` ids and ~72 chars of a long secret reached the report and the audited history. **R3** (`ed00e4e`, 128 assertions / 119 held) — the label-keyed withholding was bypassed by a greedy match **spanning** the secret (**101 distinct 20-char windows** of a 120-char secret in the report); the severity gate failed **OPEN**; and **a regression `749fa78` had introduced** — `MappingProxyType` is not a `dict` subclass, so `task_resolver.redact`'s `isinstance(obj, dict)` dispatch **silently stopped redacting** evidence written to the append-only history. **R4** (`79062c4`, 201 / 192) — a `.gitmodules` submodule-URL password written **verbatim** into step evidence and copied unredacted into the report artifact, beside a finding text that *was* redacted. **R5** (`8799db9`, 451 / 439) — seven credential families the tables never **matched** left `credential_reads` PASS while the code and docs claimed no embedded credential could; families added, claim narrowed, gap named **R7**. **CLOSED at `8799db9`**, os_lab **255** (catalog 83 · runtimes 92 · certification 80). **No refuter is in flight and no cell is open** — what remains is the named residual R7 |

---

## How to use this matrix
1. A requirement is **SATISFIED** only when its row shows real artifacts in the tiers §134 requires
   for its class (unit + adversarial at minimum) **and** the §161 ledger agrees. SATISFIED never
   implies deployed or enabled.
2. `[G:*]` gate tags mirror the ledger: `STAGING` (no isolated staging environment), `OPS`
   (operator-only act), `SPEC90` (truncated spec tail).
3. Never fill a cell from expectation. Paste the real evidence pointer (test module + count, review
   verdict, probe result, commit SHA) — and where a proof is still outstanding, say so in words.
4. `SHA` records where the evidence was taken: `4fbfb8e` = the certified prod floor (§2 only);
   `d881cf2`…`8799db9` = commits on `feat/kai-cyber-operations`, all undeployed. Where a cell still
   names `83f6050` it means the measurement was taken there and has not been re-run — said in words,
   not upgraded by assumption.
