# ============================================================
# requirements-kdp.txt — Railway KDP scheduler service deps
# ============================================================
# Pulled from main requirements.txt + Playwright (which is commented
# out there because the main API doesn't need a browser).

openai>=1.30.0,<2.0.0
anthropic>=0.34.0
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.2.0

# Cover generation + interior PDF
pillow>=10.0.0
reportlab>=4.0.0

# Telegram notifications
python-telegram-bot>=21.0

# Browser automation
playwright>=1.45.0
