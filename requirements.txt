# ============================================================
# WheellsVerse Bot Ecosystem — requirements.txt
# Compatible with macOS (Apple Silicon + Intel)
# ============================================================

# Core AI
openai>=1.30.0,<2.0.0

# Environment management
python-dotenv>=1.0.0

# HTTP & Web scraping
requests>=2.31.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
# playwright — local only (browser automation)

# Scheduling
schedule>=1.2.0
APScheduler>=3.10.4

# Data processing
pandas>=2.2.0
numpy>=1.26.0

# File handling
openpyxl>=3.1.0
python-docx>=1.1.0
PyPDF2>=3.0.0
Pillow>=10.3.0
ffmpeg-python>=0.2.0

# Email

# CLI & display
rich>=13.7.0
click>=8.1.7
typer>=0.12.0
tabulate>=0.9.0

# Database / storage
tinydb>=4.8.0
sqlite-utils>=3.36.0

# API integrations
stripe>=9.9.0
tweepy>=4.14.0

# Real data integrations
yfinance>=0.2.40,<0.2.60
pytrends>=4.9.2
feedparser>=6.0.11

# Notifications
plyer>=2.1.0

# Web server (dashboard) — FastAPI stack
fastapi>=0.116.0    # bumped from <0.116 to allow starlette CVE fix
starlette>=0.46.0   # CVE-2024-47874 (multipart DoS) fixed in 0.40+; FastAPI 0.116+ requires <0.47
uvicorn[standard]>=0.29.0,<0.35.0
flask>=3.0.0
flask-cors>=4.0.0

# Utilities
python-dateutil>=2.9.0
pytz>=2024.1
colorama>=0.4.6
tqdm>=4.66.0
pyyaml>=6.0.1
jinja2>=3.1.4

# Text processing
tiktoken>=0.7.0
nltk>=3.8.1
textstat>=0.7.3

# PDF generation
reportlab>=4.2.0
# weasyprint — local only (PDF generation)

# Markdown
markdown>=3.6.0

# ── New integrations (added 2026-03) ─────────────────────────────────────────

# Reddit automation
praw>=7.7.1

# YouTube Data API v3 + NarAI Godmode Google OAuth
google-api-python-client>=2.130.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# NarAI Godmode — encrypted vault (Fernet symmetric encryption for stored OAuth tokens)
cryptography>=42.0.0

# ── New integrations (v2.1 — 8 new bot categories) ──────────────────────────

# Claude / Anthropic API
anthropic>=0.39.0,<1.0.0

# Twilio (Receptionist bots — voice + SMS)
twilio>=9.0.0

# SendGrid (Email receptionist)
sendgrid>=6.11.0

# Hunter.io email validation (Affiliate Scout)
pyhunter>=1.7.0

# WebSocket support (Live Chat bot)
websockets>=12.0
# deploy-1776700000

# ── Wave 2 additions ─────────────────────────────────────────────────────────
python-multipart>=0.0.20  # CVE-2024-53981 (DoS via slow uploads) fixed in 0.0.18+
duckduckgo-search>=6.0.0

# ── Wave 3 additions ─────────────────────────────────────────────────────────
sentence-transformers>=2.7.0
pywebpush>=2.0.0
python-telegram-bot[webhooks]>=20.0
discord.py>=2.3.0
PyNaCl>=1.5.0  # required by discord.py for voice channels (DISCORD_VOICE_ENABLED=true)
PyGithub>=2.1.0
google-generativeai>=0.7.0
mistralai>=1.0.0

# ── NarAI User Platform (Supabase auth + DB) ─────────────────────────────────
supabase>=2.4.0

# ── NarAI v2 auth (JWT + bcrypt password verification) ───────────────────────
bcrypt>=4.0.0
passlib[bcrypt]>=1.7.4
PyJWT>=2.8.0

# ── NarAI v2 chat/memory/rag pipeline (Steps 1–7) ────────────────────────────
# chromadb backs the episode store and RAG; litellm is the LLM router;
# sqlalchemy[asyncio]+aiosqlite back the facts/patterns SQL store.
chromadb>=0.5.0
litellm>=1.83.0   # GHSA-jjhc-v7c2-5hh6 (Critical) + 3 High advisories survive through 1.80; bumping floor
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0

# ── NarAI v2 voice (TTS; STT uses openai package above) ──────────────────────
edge-tts>=6.1.0

# ── Server utilities ──────────────────────────────────────────────────────────
psutil>=5.9.0
cachetools>=5.5.0   # per-user BrainClient TTL cache in narai/api/dependencies/brain.py

# ── NAI / NarAI (Stage 0) ─────────────────────────────────────────────────────
# openai/anthropic/httpx/tiktoken already pinned above with compatible floors.
pgvector==0.3.6
sse-starlette>=2.1.0,<3.0.0   # 3.x bumps starlette to 1.0 which breaks fastapi 0.115.x
