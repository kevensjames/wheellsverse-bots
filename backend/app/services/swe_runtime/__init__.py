"""KAI software-engineering runtime — a disposable-container sandbox for running
bounded, operator-approved code-execution tasks. Feature-flagged OFF by default
(KAI_SWE_RUNTIME_ENABLED); never on the production daemon. See docs/swe_runtime.md."""
